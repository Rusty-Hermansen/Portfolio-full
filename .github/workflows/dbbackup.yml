name: "db_make_backup"

# on:
#   schedule:
#     - cron: "*/1 * * * *" 

on: 
  push:
    branches: [main]

jobs:
  build: 
    name: db_make_backup
    runs-on: [self-hosted, db_make]
    steps: 
      - name: backup_database
        run: |
          cd /home/rusty/backups
          docker exec -t portfolio_database pg_dumpall -c -U postgres > dump_`date +`%m-%d-%Y"_"%H_%M_%S`.sql
